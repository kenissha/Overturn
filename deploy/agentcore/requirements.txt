# Dependencies of the extraction runtime on Amazon Bedrock AgentCore.
# The overturn package itself is deployed as source from the repository root.
pydantic>=2.7
PyYAML>=6.0
strands-agents>=1.0
boto3>=1.34
bedrock-agentcore>=1.0
